# Shell scripts must keep LF endings or they fail on macOS, Linux and inside containers,
# even when committed from a Windows machine with core.autocrlf=true.
*.sh     text eol=lf
mvnw     text eol=lf

# Windows batch files expect CRLF.
*.bat    text eol=crlf
*.cmd    text eol=crlf
